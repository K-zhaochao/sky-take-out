<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into sky_take_out.dish(name, category_id, price, image, description, status, create_time, update_time, create_user, update_user)
        VALUES (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <delete id="deleteByIds">
        delete from sky_take_out.dish where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=", ">
            #{id}
        </foreach>
    </delete>

    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        select d.*, c.name as categoryName
        from sky_take_out.dish d left join sky_take_out.category c on d.category_id = c.id
        <where>
            <if test="name!=null and name!=''">d.name like concat('%', #{name}, '%') </if>
            <if test="categoryId!=null">and d.category_id=#{categoryId} </if>
            <if test="status!=null">and d.status=#{status}</if>
        </where>
        order by d.create_time desc
    </select>

    <update id="update">
        update sky_take_out.dish d
        <set>
            <if test="name!=null and name!=''">d.name = #{name},</if>
            <if test="categoryId!=null">d.category_id = #{categoryId},</if>
            <if test="price!=null">d.price = #{price},</if>
            <if test="image!=null">d.image = #{image},</if>
            <if test="description!=null">d.description = #{description},</if>
            <if test="status!=null">d.status = #{status},</if>
            d.update_time = #{updateTime},
            d.update_user = #{updateUser}
        </set>
        where id = #{id}
    </update>
</mapper>
